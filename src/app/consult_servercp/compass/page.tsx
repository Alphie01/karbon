import EcoCompassWizard from "@/components/consult/EcoCompassWizard";

export default function CompassPage() {
    return (
        <div className="min-h-full p-4 md:p-8 flex items-center justify-center">
            <EcoCompassWizard />
        </div>
    );
}
